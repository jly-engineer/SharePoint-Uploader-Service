# SharePoint-Uploader-Service
This native Windows Service automates local-to-cloud file sync. It monitors folders recursively, uploading new data to SharePoint via the Graph API. Designed for stability, it handles file locks from scanners, filters junk files like Thumbs.db, and mirrors directory structures. Includes a standalone GUI installer for easy deployment.
